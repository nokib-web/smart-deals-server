/**
 * after login : server will create a jwt token
 * 
 * 2. store it in the client side(localstorage, httponly cookies, in memory)
 * 3. fro asking fro sensitive send request with jwt token in the header
 * 4. server will verify the token. if the token is valid then will provide the data
 * 
 * 
 * 
 * 
 * ===========access token vs refresh token===============
 */